language: python
python:
- '3.5'
- '3.6'
install:
- pip install pipenv
- pipenv install --dev
script: pipenv run py.test -vv
branches:
  only:
    - master
notifications:
  email:
    on_failure: always
    on_success: never
